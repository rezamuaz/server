let express,join,ApolloServer,error,success,DB,HOST,PORT,AppModels,typeDefs,resolvers,AuthMiddleware,ApolloServerPluginLandingPageProductionDefault,ApolloServerPluginLandingPageLocalDefault,ApolloServerPluginLandingPageGraphQLPlayground,mongoose;_e20‍.w("express",[["default",["express"],function(v){express=v}]]);_e20‍.w("path",[["join",["join"],function(v){join=v}]]);_e20‍.w("apollo-server-express",[["ApolloServer",["ApolloServer"],function(v){ApolloServer=v}]]);_e20‍.w("consola",[["error",["error"],function(v){error=v}],["success",["success"],function(v){success=v}]]);_e20‍.w("./config",[["DB",["DB"],function(v){DB=v}],["HOST",["HOST"],function(v){HOST=v}],["PORT",["PORT"],function(v){PORT=v}]]);_e20‍.w("./models",[["*",null,function(v){AppModels=v}]]);_e20‍.w("./graphql/typeDefs",[["default",["typeDefs"],function(v){typeDefs=v}]]);_e20‍.w("./graphql/resolvers",[["default",["resolvers"],function(v){resolvers=v}]]);_e20‍.w("./middlewares/auth",[["default",["AuthMiddleware"],function(v){AuthMiddleware=v}]]);_e20‍.w("apollo-server-core",[["ApolloServerPluginLandingPageProductionDefault",["ApolloServerPluginLandingPageProductionDefault"],function(v){ApolloServerPluginLandingPageProductionDefault=v}],["ApolloServerPluginLandingPageLocalDefault",["ApolloServerPluginLandingPageLocalDefault"],function(v){ApolloServerPluginLandingPageLocalDefault=v}],["ApolloServerPluginLandingPageGraphQLPlayground",["ApolloServerPluginLandingPageGraphQLPlayground"],function(v){ApolloServerPluginLandingPageGraphQLPlayground=v}]]);_e20‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



// const  schemaDirectives = require ('./graphql/schema/directives');












const cors = require("cors");
const { graphqlUploadExpress } = require("graphql-upload");

const app = express();
// Remove x-powered-by header
app.disable("x-powered-by");
var whitelist = ["http://localhost:3000"];
var corsOptionsDelegate = function (req, callback) {
  var corsOptions;
  if (whitelist.indexOf(req.header("Origin")) !== -1) {
    corsOptions = { origin: true }; // reflect (enable) the requested origin in the CORS response
  } else {
    corsOptions = { origin: false }; // disable CORS for this request
  }
  callback(null, corsOptions); // callback expects two parameters: error and options
};
app.use(cors());
app.use(express.static(join(__dirname, "./uploads")));
app.use(graphqlUploadExpress());
// app.use((req, res, next) => {
//   res.setHeader("Access-Control-Allow-Origin", "*");
//   res.setHeader("Access-Control-Allow-Methods", "POST,GET,OPTIONS");
//   res.setHeader("Access-Control-Allow-Headers", "Content-Type, Authorization");
//   if (req.method === "OPTIONS") {
//     return res.sendStatus(200);
//   }
//   next();
// });
app.use(AuthMiddleware);

const server = new ApolloServer({
  cors: true,
  typeDefs,
  resolvers,
  plugins: [
    process.env.NODE_ENV === "production"
      ? ApolloServerPluginLandingPageProductionDefault({
          graphRef: "my-graph-id@my-graph-variant",

          footer: false,
        })
      : ApolloServerPluginLandingPageLocalDefault(),
  ],
  context: ({ req }) => {
    let { user, isAuth } = req;
    return {
      req,
      user,
      isAuth,
      ...AppModels,
    };
  },
});
// Function to start express and apollo server
const startApp = async () => {
  const host = "0.0.0.0";
  const port = process.env.PORT || 5000;
  _e20‍.g.console.log(DB);
  try {
    // Connect With MongoDB Database
    mongoose.connect(DB, {
      useNewUrlParser: true,
      useUnifiedTopology: true,
    });
    success({
      badge: true,
      message: `Successfully connected with the database`,
    });
    await server.start();
    // Apply Apollo-Express-Server Middlware to express application
    server.applyMiddleware({
      cors: true,
      app,
    });
    // Start Listening on the Server

    app.listen(process.env.PORT || 5000, () =>
      success({
        badge: true,
        message: `🚀 Server ready at http://localhost:${PORT}${server.graphqlPath}`,
      })
    );
  } catch (err) {
    error({
      badge: true,
      message: err.message,
    });
  }
};

// Invoke Start Application Function
startApp();

// DB=mongodb://127.0.0.1:27017/server
