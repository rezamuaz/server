let pick,sign,SECRET;_11e‍.x([["issueAuthToken",()=>issueAuthToken],["serializeUser",()=>serializeUser]]);_11e‍.w("lodash",[["pick",["pick"],function(v){pick=v}]]);_11e‍.w("jsonwebtoken",[["sign",["sign"],function(v){sign=v}]]);_11e‍.w("../config",[["SECRET",["SECRET"],function(v){SECRET=v}]]);





       const issueAuthToken = async (jwtPayload) => {
  let token = await sign(jwtPayload, SECRET, {
    expiresIn: 3600 * 24,
  });
  return `Bearer ${token}`;
};

       const serializeUser = (user) => pick(user, ["_id", "username", "role"]);
